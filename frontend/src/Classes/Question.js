export class Question
{
    constructor(id, number, text, property_set_id)
    {
        this.id = id;
        this.number = number;
        this.text = text;
        this.property_set_id = property_set_id;
    }
}