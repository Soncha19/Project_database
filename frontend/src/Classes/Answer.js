export class Answer
{
    constructor(id, number, text, feedback_id)
    {
        this.id = id;
        this.number = number;
        this.text = text;
        this.feedback_id = feedback_id;
    }
}